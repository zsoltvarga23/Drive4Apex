[build]
  command = "yarn build"
  publish = "dist"

# Shared-leaderboard backend (Netlify Function + Netlify Blobs).
[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
